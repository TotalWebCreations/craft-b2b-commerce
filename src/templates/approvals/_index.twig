{% extends '_layouts/elementindex' %}

{% requirePermission 'b2b-commerce:manageApprovals' %}

{% set title = 'Approvals'|t('b2b-commerce') %}
{% set crumbs = [
    { label: 'B2B'|t('b2b-commerce'), url: url('b2b') },
] %}
{% set elementType = 'totalwebcreations\\b2bcommerce\\elements\\Approval' %}
{% set canHaveDrafts = false %}

{# Read-only monitoring: approval decisions belong to the company's own approvers, never the store
   operator. The element carries no create/save/delete capability (see Approval::canCreate/canSave/
   canDelete), so this native index observes the queue but never resolves from here. #}
